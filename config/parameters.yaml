parameters:
    app.languages:
        'en': { code: 'en', name: 'English' }
        'es': { code: 'es', name: 'Español' }

    app.user_preferences:
        sidebar_collapsed:
            type: boolean
            default: false
            label: 'Sidebar collapsed'
        locale:
            type: string
            default: 'en'
            label: 'Language'
        brand_override:
            type: string
            default: ''
            label: 'Brand override (super admin only)'
