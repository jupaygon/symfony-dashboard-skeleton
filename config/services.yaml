imports:
    - { resource: parameters.yaml }
    - { resource: brands.yaml }
    - { resource: user_preferences.yaml }

parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Domain/Model/'
            - '../src/Domain/ValueObject/'
            - '../src/Kernel.php'

    # Domain port bindings
    App\Domain\Port\UserRepositoryInterface:
        alias: App\Infrastructure\Persistence\Doctrine\UserRepository

    App\Domain\Port\UserPreferenceRepositoryInterface:
        alias: App\Infrastructure\Persistence\Doctrine\UserPreferenceRepository

    # Branding
    App\Infrastructure\Branding\BrandResolver:
        arguments:
            $brandDefs: '%brands_defs%'
            $brandMap: '%brands_map%'
            $defaultBrand: '%brands_default%'
            $devSuffixes: '%brands_dev_allowed_suffixes%'

    # User preferences
    App\Application\Service\UserPreferenceService:
        arguments:
            $definitions: '%user_preferences%'
