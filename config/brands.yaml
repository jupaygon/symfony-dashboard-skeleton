parameters:
    brands_default: 'default'
    brands_map:
        # hostname: brand_key
        # 'dashboard.example.com': 'default'
        # 'dark.example.com': 'jarvis'
    brands_defs:
        default:
            name: 'Dashboard'
        jarvis:
            name: 'Dashboard (Dark)'
